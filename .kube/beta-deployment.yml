---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    type: beta-$ServiceName
    version: primary
  name: beta-$ServiceName
  namespace: $Namespace
spec:
  selector:
    matchLabels:
      type: beta-$ServiceName
  template:
    metadata:
      labels:
        type: beta-$ServiceName
        version: primary
    spec:
      serviceAccountName: beta-$ServiceName-sa
      containers:
        - name: beta-$ServiceName
          env:
            - name: NODE_ENV
              value: beta
            - name: APP_NAME
              value: $ServiceName
            - name: LOG_LEVEL
              value: debug
          image: <ECR_REPO_NAME>
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: <Health_check_path> # The path to your health check endpoint
              port: 3000 # The port on which your NestJS application runs
            initialDelaySeconds: 180 # Delay before the first probe is initiated
            periodSeconds: 20
          resources:
            limits:
              cpu: 1
              memory: 1800Mi
            requests:
              cpu: 1
              memory: 1000Mi
      dnsConfig:
        options:
          - name: ndots
            value: "1"
      restartPolicy: Always

---
apiVersion: v1
kind: Service
metadata:
  name: beta-$ServiceName
  namespace: $Namespace
spec:
  ports:
    - name: beta-$ServiceName
      port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    type: beta-$ServiceName
  type: ClusterIP
