apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    type: beta-$ServiceName
    version: primary
  name: beta-$ServiceName-config
  namespace: $Namespace
data:
  app_port: ':8080'
  app_name: beta-$ServiceName
  app_version: 1.0.0
  application: beta-$ServiceName
  dd_service_name: beta-$ServiceName
  service_name: beta-$ServiceName
  aws_region: ap-south-1
---

apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    type: beta-$ServiceName
    version: primary
  name: beta-$ServiceName
  namespace: $Namespace
spec:
  replicas: 1
  selector:
    matchLabels:
      type: beta-$ServiceName
  template:
    metadata:
      labels:
        type: beta-$ServiceName
        version: primary
    spec:
      serviceAccountName: beta-$ServiceName-sa
      containers:
        - name: beta-$ServiceName-http-server
          image: 287726214764.dkr.ecr.ap-south-1.amazonaws.com/niyox2/$ServiceName:$IMGTAGHTTPSERVER
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: beta-$ServiceName-config
---

apiVersion: v1
kind: Service
metadata:
  name: beta-$ServiceName
  namespace: $Namespace
spec:
  ports:
    - name: http-beta-$ServiceName
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    type: beta-$ServiceName
  type: ClusterIP

---